# Gloom Bean agent entry point

Before any continuation read START_HERE.md and Documentation/Continuity/AGENT_CHECKPOINT.md, then run `python Tools/agent_status.py --probe-write`. Hydrate current canonical cloud-context and Context/HOME.md as required.

Do not infer lost access from a missing historical tool name. Discover the current connector schema and perform one bounded scoped probe. Do not reimplement completed Parish/Orchard work from old delivery summaries.

Commit each meaningful owned unit before long builds or session boundaries. Use Tools/checkpoint_work.py with explicit paths and expected HEAD. Preserve WIP and failures honestly. Never stage another worker's files. Local commits, bundles, context and remote game-code publication are separate states.

The first unmet gate and tested revision are in Documentation/Continuity/CURRENT_CHECKPOINT.json. Preserve the complete original atlas finish line. No paid Codex CLI workers or app-only artifacts.
