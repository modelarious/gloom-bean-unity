# Q11 writable workspace

The SentinelX direct editor rejected writes to the older C:/Users/micha project because file_ops exposes that subtree as read-only. Policy was inspected without revealing credentials: C:/ProgramData/SentinelX/workspace is explicitly rw. No agent policy was expanded and no denied original write was retried through another tool.

Q11 continues in a new ordinary clone at C:/ProgramData/SentinelX/workspace/GloomBean-Q11, branch visual/qualitybar-q11, preserving the recovered0cf69a commit and all history. Original QualityBar Q1 remains unchanged after the denial. Authorized GitHub SSH publication remains available. The new working branch isolates the iteration rather than overwriting the older checkout. All subsequent writes target the explicit rw workspace.

The current scope is authored patrol/boss art and read-only state-driven animation. The five boss and ten patrol sheets retain fixed actor footprints; movement, collisions, saves, abilities and level builders do not read the art. All225 retained viewpoints and current native route checks are required. The whole-game concept-quality target remains UNMET.
