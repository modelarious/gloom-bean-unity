# Q9 continuation — expression and constructed environments

Continue the published Q8 implementation. The supplied concept remains a target, not game evidence. The checkpoint and Hindsight recall agree on a4386033; public push works. No host/repository/chapter reconstruction is required.

Contract: keep the original complete game, permanent-corruption hierarchy, optional secrets and GBA-style native pixels. No review-coordinate decorations, generated screenshot claims, paid workers or retired-system writes. Git-first, Hindsight-second continuity. Existing global contract applies; project CONTRACT.json is absent, not a failed user requirement. Automatic acquisition of all chats remains UNKNOWN and is irrelevant to the available source.

Owns: new QualityBar expression/architecture assets and scripts; HostPixelArt.Reference and HostPixelView visual state selection; QualityBarWorld visual children and lifecycle tests; explicitly added broad capture holdouts; documentation/report helpers. Do not change ActorMotor, input, enemies' gameplay, progression, stage builders, possession rules or old references.

- [ ] S1: Frame-authored expressive motion uses actual actor state across all25 stages. Original and corrupted identities remain distinct; form overlays remain recognizable. No simulation or collider change.
- [ ] S2: Structural machinery and room layers attach to actual floor/rail/moving-object bounds throughout applicable stages, not only known samples. Disabled/late-spawned objects obey lifecycle and readability.
- [ ] S3: Retain175 existing views and52 newer WL4 references. Add unseen holdout locations where distinct supports exist, and capture actual movement rather than only static staged figures.
- [ ] S4: Inspect the first revised native output, correct observed regressions and recapture. Keep negative images. Every-stage coverage is required but not an art-completion certificate.
- [ ] S5: Native tests cover actor pose selection, all-stage application and prior mechanics/routes; exact packaged actions/endings are run on the new build. Source identities remain separate.
- [ ] S6: Native visuals reach the concept/extreme-fidelity target throughout the game. UNMET until actual evidence supports it; do not grade scripted coverage as beauty.
- [ ] S7: Publish source, real before/after/action evidence, playable ZIP and precise current checkpoint. Git -> Hindsight -> user report. No background work promised after delivery.

Implementation focus: the actor currently changes mostly by foot-row offsets and body rotation. Author distinct stride, launch, fall, brace, pound, swim, carry and hurt poses at native-scale pixel detail. Then improve the sparse long-rail/backstage space with layered pulley/pipe/hanger construction; interior bays need tangible room components instead of repeated window slabs. The same render rules must remain visible outside sampled cameras.
