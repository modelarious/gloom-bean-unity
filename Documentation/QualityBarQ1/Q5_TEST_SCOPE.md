# Current quality layer native checks

The existing all25-stage native traversal of construction fixtures is extended with75 assertions: each actual stage has the quality component, each eligible object has applied art and no added collider; a newly spawned support at(-1000,-1000), outside every screenshot, receives the same artwork; disabling that source hides its decorative children. These are rendering consistency checks and are not artistic-quality or reachability tests.

Original BroadVisual checks remain. Actual whole-game stage logic and current focused input routes are tested separately. No route grants, movement changes or weaker gameplay assertions are introduced.
