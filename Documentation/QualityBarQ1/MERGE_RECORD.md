# Reconciliation with concurrent visual/v6 work

Merged pinned64cf65bdf9e6be32dec09a028a1bde2c1f8d4768 into the isolated qualitybar branch. The original live worktree was not checked out/reset/edited. This retains its source/late-spawn classification repairs, living boss heart and exposed core art, render-only boss camera bounds, menu scenery, native all25-stage checks and original raw evidence.

The single source conflict was BroadVisualCapture: incoming adds D boss-focused validation and E final-generalization views. All original75 A/B/C anchors remain byte-for-byte in the sampling formula; D/E are additional, not replacements or invented before-images. Q coverage fields are retained across all125 images. Fixed camera/body/form staging remains explicit. No rule, collider, source trigger, possession, save or test assertion was removed to make the merge pass.

Next: Q3 connects the new object assets, addresses Q2 material-role defects and re-runs the full125-image corpus plus native checks against this actual combined source. Neither earlier worker test certificate is relabelled as a certificate of the merged runtime.
