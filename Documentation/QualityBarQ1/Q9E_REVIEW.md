# Q9E native review — reject rectangular leaf fragments

The outdoor warehouse motif was correctly removed. A full-size unseen Orchard frame then exposed the next defect: rectangular crop boundaries in the reused foliage fragments. Q9F masks each texture fragment to an irregular stepped native-pixel crown before composition, preserving actual source alpha and sharp pixels. No blurred edge, level-specific camera hack, new collider or gameplay change is used. The original Q9E image remains available; the crop defect is not called polish.

All225cameras and the current13scenario native suite remain separate evidence. The supplied concept bar is still unmet; the revision is accepted only after another actual capture at the same location.
