The initial Q9 gate commit c470d4c was preserved before the tool deadline during expensive full-history bundling. It was then pushed and independently matched by remote ref. With the public origin working, subsequent owned visual commits use explicit expected-HEAD/staged-path checks plus push/ref verification instead of rebuilding the entire multi-hundred-MB history bundle on every art edit. Incomplete older bundle material is retained. This is not lost write access.
