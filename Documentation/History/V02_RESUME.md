# Resume the existing Unity implementation

Do not recreate the project or recover it from chat summaries. Start from the shipped source and Git bundle; the original foundation tag precedes the atlas. Current detailed status is RELEASE_STATUS.md and original constraints remain in Source/CURRENT_REQUEST.txt and the pinned 44-page atlas.

On gamer-bro the main worktree is C:/Users/micha/Projects/GloomBeanUnity; the separate base is C:/Users/micha/Projects/GloomBeanFoundation. These are observed execution locations, not replacements for canonical project resolution. Engine-tested atlas code is 513edc626c5ee97f65af5ec4855c3fae7a19264e. Foundation-v0.1.1 is ab2d9ec420fd263ffec5a47d1754e34c8ee11fde.

Open Boot.unity with Unity 6000.5.9f1 or use Gloom Bean > Campaign workbench. Tune MovementTuning.asset. Edit persistent levels in AtlasCampaign.* builders; a runtime scene export does not preserve callback behavior as production authoring.

Next actions: extend RouteVerification's genuine-input route to Belfry, resolve timing and source/cure failures, and add all remaining critical paths and boss victories. Then validate optional Mercy solutions without turning them into mandatory gates. Preserve original requirements; do not substitute construction checks for completion. A playtester must separately evaluate physical-controller mapping, feel, readability, difficulty and pacing.

Run Tools/Build-Windows.ps1 and Tools/Verify-Windows.ps1 with an already licensed ordinary desktop user. Both support paths with spaces and bounded process timeouts. Use Mechanics or OpeningRoute suite. Logs are unique per run; test saves are isolated. The source contains a short pendulum probe under -gb-route-verify -gb-boss-probe for the world-anchored-joint collision regression.

GitHub factory request #37 was blocked by HTTP401. Do not announce remote publication or invent a replacement repo. Git bundles are the complete local-history transport. Canonical context ingress and its acknowledgement are a separate publication state.
