# Measured route corrections
H09 completed L17 ordinary return at120fps and delayed start, while30fps entered a reversing orbital field during its first return launch. The witness now responds to the actual local pole only while inside that orbital field; fixed-coil launch inputs are unchanged. L18 both outward shadow latches worked through real silhouettes, but the final wide landing created a1.2m headroom trap over the prior step. Narrowed that landing so the existing1.5m body has a valid jump approach; no controller or jump upgrade.
