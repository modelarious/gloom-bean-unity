# Subsequent launch circuits
H01 fixed-step magnetic poses and choir timing crossed the first two aerial gaps in the observed run. The third had no nearby fixed launch anchor and dropped into the lower recovery floor. Added visible reversible local coil circuits at later docks with the existing170strength/range, rather than changing jump/force stats. The player must operate the real lever and choose the pole. These are staged implementation changes; signature screen/bell exchange and orbiting Mercy remain separate fidelity work, not declared solved. H02 request is separate until H01 is idle.

The magnetic witness now evaluates its input policy on every actual motor Consume call rather than only when the render coroutine resumes. It reads production state and emits ordinary controls; it never sets position/velocity/forms. H02 checks30/120fps plus a2.1second idle start. These are tests, not a new player control scheme.
