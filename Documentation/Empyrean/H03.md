# Launch geometry and observed-state controls
H02 demonstrates that narrow unanchored first launches remain phase-sensitive and a high optional magnet perturbed the main third gap. The opening dock now teaches the same explicit fixed-coil launch used later; the optional upper route is above the critical field corridor. Flight witness follows a rising transit corridor then an actual support rather than braking at arbitrary point coordinates. No forces/velocities/positions/grants are injected; all commands go through the ordinary motor input. Secret test explicitly remains unmet until the actual orbit solution is implemented.
