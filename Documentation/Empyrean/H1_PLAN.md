# World 5 continuation — h1

Scope remains the original atlas, pp36–41 and possession catalog. Existing sixteen-level/four-boss release is immutable and separate. Current source recovered at1149326; no missing write access or active job.

## Acceptance inventory

1. L17: input-only ordinary/secret/Turn route with reciprocal forces, real catch and momentum exchange; repeated start delays must not turn a single lucky trajectory into acceptance.
2. L18: actual cast-shadow topology, controllable screens, fixed-overhead-sun Turn and body-cast secret.
3. L19: delayed temporary ink, physically relocated punctuation affecting layout, fresh-route return and shadow/tether secret.
4. L20: distinct inverted sanctums and genuinely alternative solution families; final Mercy rhythm; controlled collapse.
5. Final boss: real three-act choice/steal/combination behavior; normal vs all20-Mercy endings, with corruption never cured during ordinary play.
6. Native regression, earned-save chain, isolated practice, negative controls, committed evidence, retrievable full Git history, current precise continuation.
7. Production presentation and human/controller acceptance remain separate open gates, not silently satisfied by scripted checks.

## First diagnosed production defect

Lodestone sampled interpolated Transform positions while applying forces to fixed-step rigid bodies. Halo polarity was changed in Update rather than the physics step. Both can change the physical input to the force law with rendering cadence. h1 moves force sampling and choir transitions into fixed-step semantics; it does not upgrade strength, range, jump speed, or teleport anything. Route outcomes still need native measurement.

Technical reference: Unity official Rigidbody2D.position, RigidbodyInterpolation2D and SimulationMode2D.FixedUpdate documentation. Render interpolation is presentation, not simulation authority.
