# Input-edge correction
H07 completed all five outward coil flights twice, but at30fps extra opposite-pole ticks before launch pulled the body off its launch stance; leaving the final anchored coil also required switching it off. The next witness positions while the circuit is OFF, then operates E/pole/jump on a consumed physics input. Actual ordinary controls still make all state changes. It uses the visible fixed pole rather than guessing polarity from a future target. Shadow geometry/core suite from H07 is still independently running.
