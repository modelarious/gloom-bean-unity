The existing GloomBean-Orchard-v04 scheduled action still invokes its machine-local Run-Orchard-Local.ps1. That file now delegates to the committed Tools/Run-Campaign-Checks.ps1. The portable implementation owns all build, timeout, stage-suite dispatch and receipt logic. The request remains Tools/orchard-request.json. A staged Git operation refused the ignored local runner; only this session's reviewed paths were unstaged and recommitted. The ignored wrapper is not the source of truth.
