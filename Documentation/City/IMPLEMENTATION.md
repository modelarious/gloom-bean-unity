# City implementation notes

L09 R01 proved the stacked solid balconies create a headroom trap. Real one-way balcony surfaces now preserve the floor while permitting approach from below. Mirror acquisition now copies and mirrors the actual initial movement state; subsequent collisions remain independent.

L09 now contains the atlas's paired apartment lift, actual kinematic shutter displacing only the reflected body for an optional off-register Mercy, curtains whose occlusion creates cast-shadow bridges, and a genuinely different Mirror route after the eastern sun is extinguished. Bodies are not teleported to scales; gate/secret outcomes are measured from physical mass.

Original failure kept under Evidence/R01. Native acceptance is pending this revision. No human-readability/final-art claim. Remaining L10-L12 and Surveyor are not certified.

R02: 200 mechanics checks passed. Mirror initializes symmetrically; the real cabinet makes the reflected body land later. R03 waits for that physical landing before the next shared jump, and stops the witness before querying inactive return objects after a failure. No world state or body position injection.

R03 established both mirror scales and physical shutter displacement. A matte courtyard curtain now allows the original body to leave after solving the two-body apartment; unlike disabling twin collision, this preserves the atlas collision cost and visible cure. Secret witness waits for the entire shutter motion instead of treating earlier plate overlap as completed movement.

L10 implementation: a real complementary tile topology now supports repainting. A traveling scaffold completes an optional moon outline by changing both physical domains, rather than unlocking a color-key door. Shared apertures preserve frame collision; the mirror court uses a non-overlapping pair of scales and an asymmetric paint pot. Return gallery and roof support authored. Real-input route and secret witnesses added; native result still pending.

Parallax now changes its actual capsule atomically around screen-space centre, with the destination collision mask; blocked growth preserves size, pose and depth. Up/down depth input no longer accidentally crouches the body afterward. Shared centre-resize primitive and three physical assertions are mirrored into the independent foundation. Unity OverlapCapsuleAll is used for the real target shape. Validation pending.

R05: L09 completes both paired routes, source/cures, lift, shadow bridges and the Mercy; its final exit uses E to lower the same lift rather than waiting on a stationary upper stop. L10 revealed tight head clearance on the rising interior corners. Enlarged the painted stone shaft upstream of each step, retaining the complementary route and full-size actor rather than granting clipping.

L11 authored: three collision/scale planes, overlapping transfer spaces, a clerk who stamps real furniture, an enlarged filing-cabinet interior, and return counters whose depth differs after office closure. Mercy registration uses two separate physical contact sensors plus a ceiling footprint, not an enum/door check. Added adaptive input-only plane jumps and ordinary/secret witnesses; not yet route-validated. L10 top inside corner also widened vertically to avoid a two-metre ceiling over a rising step.

L10 moon branch: closing an outline also draws its supporting boundary. The old fill-only edit opened space but supplied no continuous floor; the body correctly fell into the lower corridor. Added complementary boundary cells and an explicit erase-after-secret input witness, preserving reversibility. L11 witness now uses the actual far-body run-up distance at the first edge.

Hotel support primitives: room shells are real kinematic bodies, quarter-turn destinations follow discrete depth, carried props are deduplicated, and pressure scales exclude self-propelled structural scenery (including their own moving floor). Only actual dynamic weights or actor bodies count. This also removes a possible shutter-only false positive in L09. Foundation carry now follows kinematic supports in Interior/depth layers; lethal damage clamps at zero. Four new native assertions cover these shared changes. Independent foundation mirrors all shared changes. Native regression is pending.

R08 L10 completes the forward and optional closed-moon puzzle, then reaches the return aperture. Its reverse .8 m sill needs the same real jump used successfully on secret entry, not walking. L11 registration step now starts as a stowed drawer; it is extended from the mid-plane counter after crossing, so it cannot obstruct the approach's required mid-body growth. Added exact depth/mask/contact failure observations. New tests also rerun the shutter secret under the corrected dynamic-load rules.

L12 authored: preserved room shells physically orbit with depth changes; a mirrored pair occupies separate stamped depths while holding the same room brake; a previously external wall supplies an inverse collision corridor; roof projection leads to the World Nail. The return removes the facade and releases physical room motion. A real maintenance winch exposes the inside of the lobby chandelier for an optional Mercy. New witness tracks supports and room locations using movement inputs only. No teleport doors, reset health, grant states or artificial room completion. Native route acceptance pending.

R09 exposed a real simulation defect, not just a route miss: PlatformEffector2D default colliderMask overrode the platform's layer and let bodies stand on foreign depth shelves. OneWaySurface now disables that override so physics honors the global/per-body layer rules. Two new real-physics tests distinguish foreign-plane fall-through from selected-plane support. Unity official Effector2D.colliderMask documentation states that the GameObject layer is not used when its custom mask is active. The registration desk now unfolds as a complete local fixture rather than blocking the unrelated main projection arc while closed.

Hotel progression now uses measured launch edges and flush doorway floors. Partial removal of Mirror/Echo, like removal of Marionette, leaves a retained body configuration untouched and must not falsely demand full-size restoration clearance; a new paired negative/positive native check proves that full restoration still fails under the same low roof. The Hotel Turn removes the ordinary back-wall facade while retaining its independently represented inside topology. L11 far balcony has enough physical overlap for the measured small-body horizontal envelope.

Surveyor primitives: attack and ground-pound overlap queries now obey the actor collider's actual contact mask, including per-body plane overrides. A paired native test proves an excluded target survives and the identical target can be hit when its contact domain is included. This avoids a form-name damage check and makes projected weak points physically distinct. Added real hit-reactive Surveyor cores and telegraphed mirrored counterstrokes, not phase-injection buttons. Native acceptance pending.

R11 proved the complete L11 secret route. The ordinary route exposed reliance on the unfolded optional desk during the final descent; widened return-only steps now independently overlap the fall envelope. L12 paired scales could release the rotating room while both bodies were still crossing the contacts in midair. The lift now requires sustained load and an explicit E call while both scales are held; its location still moves physically. Surveyor now has actual hit-reactive reflected, inside-outline and three moving depth-caliper cores. Actor position and health are preserved across acts; no scripted teleport or phase injection. Witness implementation follows.

R12: Surveyor full three-act native input victory observed. Hotel second ride exposed a witness mistake: chasing the room centre made the two mirrored bodies walk toward one another, leave their balcony supports and land on another room. RideRoom now uses neutral inputs and requires actual support from the intended room at destination. A new physics fixture verifies neutral actor carry plus exactly-once compound-prop displacement. Original failures retained, no support assertion weakened.

R13 neutral-carry fixture exposed a real support bug: upward kinematic contact velocity was treated as the actor jumping, losing support; interpolated child transforms also supplied the wrong fixed-step reference. Support now uses Rigidbody2D physics poses, accounts for normal displacement already delivered by collision, and tests upward velocity relative to its support. Room floors remove competing friction-based tangent carry for props moved by the room. No route position injection or weakened room support assertion. Foundation mirrored; real regressions pending.

Neutral room ride still failed, so the next run records exact fixed-step actor/support/room positions and velocities rather than guessing at further movement changes. Normal carry compensation is disabled for truly stationary surfaces and an explicitly centre-preserving resize invalidates the old support reference.

R15 exact trace: the room moves 7 m/s upward while Rigidbody2D.linearVelocity reads (0,0) during its queued MovePosition, so the controller still misclassified upward solver velocity as a jump. Support velocity is now measured from consecutive fixed physics poses, not that queued Rigidbody property. Static props are no longer redundantly repositioned when the room has zero delta. Actual trace and strengthened neutral carry tests remain the oracle.
