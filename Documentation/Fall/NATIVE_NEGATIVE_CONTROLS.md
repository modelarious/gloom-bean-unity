# Native negative controls

Added a dormant return-mover fixture: it must receive TemporalBody while inactive and genuinely slow after activation. Added an inspection-lift tall-passenger fixture: the real collision footprint must block the low ceiling and the lift must return rather than clip or crush it. The already passing L15 all-secret input route is the corresponding horizontal-Coffin positive control. These tests remain independent from map construction counts.
