# P5: measured character/material weaknesses

P4 is undergoing its own pinned full54-case regression. Do not change that clone or relabel its output as a later revision. P5 keeps movement, collisions, source rules and stage logic byte-identical; asset replacements still require actual recapture and a native resource/gameplay regression.

The boss4/5 art is connected, but weak: flat repeated emoji faces do not communicate falling physical weight or a charismatic final entity. Replace their authored pixels with asymmetric, shaded, interlocking silhouettes. Preserve the collision-sized visual footprint; do not inflate the boss or invent a victory pose. The False Empyrean background is excessively grey and low-contrast. Compose its actual arches/columns from the separately credited CC0 source fragments using a bounded ivory/gold/violet bank and readable recesses. Record actual active sprite names in visual reports so connected rendering is observed, not guessed from source presence.

Retain all P4 screenshots and scores. A worse P5 image must be revised or rejected, never described as an improvement just because it is new. Strict superiority remains unmet until every frozen pair is genuinely ahead.
