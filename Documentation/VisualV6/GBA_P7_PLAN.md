# P7: preserve landing space and add actual-action evidence

The P6 fixed Puppet/Orchard frames exposed a surviving layout flaw: the lower-left form-status panel sat over the feet/landing area. The form card now has one stable top-left location below health; boss altitude and base seals occupy the opposite/top edge. World/camera/collision geometry is unchanged. Capture all20frozen views again rather than substituting more favorable examples.

A new opt-in observer records full native backbuffers every4seconds during existing input-driven witnesses. It only reads active actor/camera/input state, writes screenshots and a manifest, and never changes position, physics, time, health or inputs. These action captures supplement the explicitly staged fixed-camera art fixtures; they do not replace weak frames or claim a human playtest. The observer is installed only with the dedicated CLI flag in test mode. Public normal gameplay does not execute it.

P4 full regression remains pinned to its older clean candidate; P7 runtime presentation and observer get current focused tests and exact-package ending tests using separately identified genuinely earned P4 save lineage.
