# P4 native visual decision

Keep the full-height panorama framing: the actual capture now shows the orchard canopy/chapel, the torn city in the Fall, two cathedral rose windows and both illuminated manuscript columns. This is a measured comparison against P3 at identical camera and player coordinates. Source dimensions and palette banks stay unchanged; only the scenic transform changed.

The actual-keyboard journey was repeated and its fourteen captures reviewed. Black letterboxing is now present on menus, the overlapping home footer was removed, disabled worlds explicitly say LOCKED, and the controls card displays both base verbs and the current Host rule without the earlier clipping. The active game and UI share one240x160 grid, including on a non-divisible1274x783 client window.

The independent pixel audit passed20 gameplay fixtures,14 menu/input screenshots and5 oracle controls (one-pixel mutation, blur and empty frames are rejected). The original ten collider name/type/trigger/layer inventories match the baseline. These are not an art verdict or a proof of collider bounds. The production movement, forms and stage logic are separately regression-tested.

Strict superiority remains UNMET. WL4 still has stronger hand-authored foreground composition, expressive character posing and authored per-level material detail in multiple selected examples. The new renderer, palette limits and source identities must not be described as proof that those deficits vanished. All ten originals are retained alongside explicitly labelled tighter handheld views.
