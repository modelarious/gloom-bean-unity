# Atlas implementation checkpoint

The immutable foundation-v0.1.0 tag precedes all campaign code.
The current atlas pass implements the 15 possession simulators, 20 separately authored
courses and five mechanical multi-act bosses. It is an EXPERIMENTAL graybox, not a
certified finished game or pixel-art release. The source atlas remains the design authority.

Native Unity 6000.5.9f1 compiled the full campaign on gamer-bro (compile-03.exit = 0;
compile-03.log contains GLOOM_WINDOWS_BUILD_PASS). Runtime behavioral acceptance follows
in a separate commit/report. Successful compilation does not establish puzzle solvability.

Important unresolved fidelity: final-art production; full unassisted campaign traversal;
all-secret traversal; exact atlas map geometry; some advanced stage transformations;
cinematic endings; blind readability, difficulty, timing and controller-feel acceptance.

Current controls: arrows/WASD, Space/Z jump, Shift run, J/X tackle, K/C carry/throw,
E interact, U possession action (hold for Root), I alternate, Down+I changes the focused
form in a compatible pair, L or airborne Down+J pounds. F1 controls; F2 designer notes.

Leading Echo is causally defined as immediate echo inputs and a two-second delayed
primary body, not prediction. Root follows its stored curve rather than teleporting.
Magnetism exchanges forces with free bodies; fixed anchors transfer reaction to the world.
Inside-Out uses bounded complement geometry; it does not phase through arbitrary levels.

Publication: factory request modelarious/project-factory#37 failed with HTTP 401.
Local commits are durable. Do not claim the private remote exists until it is verified.